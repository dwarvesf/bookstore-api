development:
  dialect: postgres
  datasource: user=postgres password=postgres dbname=df-bookstore-srv-db sslmode=disable

test:
  dialect: postgres
  datasource: user=postgres password=postgres port=5433 dbname=df-bookstore-srv-db-test sslmode=disable

prod:
  dialect: postgres
  datasource: host=localhost dbname=${DB_NAME} user=${DB_USER} password=${DB_PASSWORD} sslmode=disable